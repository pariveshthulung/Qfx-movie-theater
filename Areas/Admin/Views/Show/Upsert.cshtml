@model QFX.ViewModels.ShowVm.UpsertVm
<form method="post">
    <br />
    <div class="container">
        <div class="card shadow border-0">
            <div class="card-body">
                <div class="container rounded p-2">
                    <div class="row">
                        <div class="col-md-12 mb-5 mb-md-0">
                            <h2 class="h3 mb-3 text-black">@Model.Audis[0].Location.CityName </h2>
                            <div class="p-3 p-lg-5 border bg-white">
                                <div class="form-group row">
                                    @* <div class="col-md-4">
                                        <label asp-for="Date" class="text-black">Date<span
                                                class="text-danger">*</span></label>
                                        <input type="date" id="datePickerId" class="form-control" asp-for="Date"
                                            required>
                                    </div>
                                    <div class="col-md-4">
                                        <label asp-for="Time" class="text-black">City Name <span
                                                class="text-danger">*</span></label>
                                        <input type="Time" class="form-control" asp-for="Time" required>
                                    </div> *@
                                    <div class="col-md-4">
                                        <label asp-for="ShowStatus" class="text-black">Show Status <span
                                                class="text-danger">*</span></label>
                                        <select asp-for="ShowStatus" asp-items="@Model.ShowStatusList()"
                                            class="form-select">
                                            <option selected disabled>Select a Status</option>
                                        </select>
                                    </div>
                                </div>
                                <div class="form-group row">
                                    <div class="col-md-4">
                                        <label asp-for="AudiID" class="text-black">Audi<span
                                                class="text-danger">*</span></label>
                                        <select asp-for="AudiID" asp-items="@Model.AudiList()"
                                            class="form-select">
                                            <option selected disabled>Select a Audi</option>
                                        </select>
                                    </div>
                                    <div class="col-md-4">
                                        <label asp-for="MovieID" class="text-black">Movie<span
                                                class="text-danger">*</span></label>
                                        <select asp-for="MovieID" asp-items="@Model.MovieList()"
                                            class="form-select">
                                            <option selected disabled>Select a Movie</option>
                                        </select>
                                    </div>
                                </div>
                            </div>
                        </div>

                    </div>

                </div>
            </div>
            <div class="card-footer">
                <div class="col-6 col-md-3">
                    <button type="submit" class="btn btn-primary form-control">Add Show</button>
                </div>
            </div>
        </div>
    </div>
</form>
<script>
    datePickerId.min = new Date().toISOString().split("T")[0];
    datePickerId.value = new Date().toISOString().split("T")[0];
</script>
